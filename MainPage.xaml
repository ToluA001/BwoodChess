<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BwoodChess.MainPage" BackgroundColor="#272537">


    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label Text="Email: " FontSize="20" TextColor="Tan" FontAttributes="Bold"/>
            <Editor Text = "{Binding UserNameText}"/>

            <Label Text="Password: " FontSize="20" TextColor="Tan" FontAttributes="Bold"/>
            <Editor Text = "{Binding PasswordText}"/>

            <Button Text = "Login"  
                    BackgroundColor = "Lavender"/>

            <Button Text = "Sign up" 
                    BackgroundColor = "Lavender"
                    Command="{Binding SignUpcommand}"/>

            <Label Text="{Binding TLabel}" FontSize="23"/>


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
